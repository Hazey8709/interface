import React from "react";

const Search = () => {
    return (
        <main style={style.mainCont}>
            <h4>Search Page</h4>
        </main>
    );
};

export default Search;

const style = {
    mainCont: {},
};
