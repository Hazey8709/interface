import React from "react";

const Settings = () => {
    return (
        <main style={style.mainCont}>
            <h4>Settings Page</h4>
        </main>
    );
};

export default Settings;

const style = {
    mainCont: {},
};
