import React from "react";

const Menu = () => {
    return (
        <main style={style.mainCont}>
            <h4>Menu Page</h4>
        </main>
    );
};

export default Menu;

const style = {
    mainCont: {},
};
